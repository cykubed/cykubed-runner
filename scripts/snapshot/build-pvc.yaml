apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: build-pvc
  namespace: cykubed
spec:
  storageClassName: "cykubed-storageclass"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
